//just like we have window in the browser which provide some cool functionality and methods
//in node we

const mission = "learn";
if (mission === "learn") {
  console.log("Time to write some node code");
} else {
  console.log(`Is ${mission} fun?`);
}
